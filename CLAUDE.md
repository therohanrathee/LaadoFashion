# Agent Instructions

**CRITICAL DIRECTIVE:** 
At every step of development or whenever a significant architectural, schema, or state change occurs, you MUST update the `whitepaper.md` document located in the root of the project directory. 
This whitepaper serves as a reference document so that any AI taking over this project can analyze it and continue development seamlessly from any point.

All Supabase SQL queries must be documented within the codebase (e.g., in a `supabase/queries.sql` file) with chronological numbering and relevant names, as the user will execute these manually in the Supabase SQL editor.
